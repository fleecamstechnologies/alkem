NODE_ENV=development
PORT=3005

DB_HOST=localhost
DB_PORT=3309
DB_USERNAME=alkem
DB_PASSWORD=alkempassword
DB_DATABASE=alkem_portal
DB_POOL_SIZE=20

JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=8h

CORS_ORIGIN=http://localhost:5173

# Optional: bulk load-test data on `npm run seed`
# SEED_CUSTOMERS=200000
# SEED_PAYMENTS_PER_CUSTOMER=5
